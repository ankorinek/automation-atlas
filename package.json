{
  "name": "automation-atlas",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Frontend-only Home Assistant custom panel: plain-language automations, flowcharts, dependency graph, and audit.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "elkjs": "^0.9.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
